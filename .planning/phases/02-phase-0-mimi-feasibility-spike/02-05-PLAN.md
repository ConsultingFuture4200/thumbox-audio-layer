---
phase: 02-phase-0-mimi-feasibility-spike
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - docs/phase0-evaluation-2026-05-XX.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - DEV-1052
  - SM-AL-1
  - SM-AL-2
  - NC-AL-1
  - NC-AL-2
  - NC-AL-3
  - NC-AL-11
must_haves:
  truths:
    - "A single Phase 0 evaluation document exists at docs/phase0-evaluation-2026-05-XX.md, dated, signed by the operator."
    - "PRD §6.2 Phase 0 kill criteria are applied honestly to each measurement, with the verdict (PASS / FAIL / INCONCLUSIVE) explicit per criterion."
    - "A binary go/no-go recommendation for Phase 1 is published at the top of the document, not buried."
    - "Strix Halo derating uncertainty (Path Cloud) is surfaced as a confidence-discount on the recommendation, not papered over."
    - "If go: Phase 1 prerequisites and risks are listed. If no-go: project pause/kill rationale is documented and ROADMAP/STATE reflect the decision."
    - "All four prior plan SUMMARYs and result JSONs are cited by file path, not summarized away."
  artifacts:
    - path: "docs/phase0-evaluation-2026-05-XX.md"
      provides: "Phase 0 evaluation document — the M1 milestone deliverable per ROADMAP."
      min_lines: 200
    - path: ".planning/STATE.md"
      provides: "Updated to reflect Phase 2 closure (Phase 0 spike complete) and the go/no-go outcome."
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 row updated to ✅ Complete or ❌ Killed; Phase 3 status updated accordingly."
  key_links:
    - from: "docs/phase0-evaluation-2026-05-XX.md"
      to: "spike/bench/results/mimi-decode-strix-derated.json"
      via: "cited as evidence for SM-AL-1 verdict"
      pattern: "mimi-decode-strix-derated\\.json"
    - from: "docs/phase0-evaluation-2026-05-XX.md"
      to: "spike/bench/results/vram-4-concurrent-strix-derated.json"
      via: "cited as evidence for NC-AL-1 verdict"
      pattern: "vram-4-concurrent-strix-derated\\.json"
    - from: "docs/phase0-evaluation-2026-05-XX.md"
      to: "spike/bench/results/llm-on-tokens-eval.json"
      via: "cited as evidence for SM-AL-2 verdict"
      pattern: "llm-on-tokens-eval\\.json"
    - from: ".planning/STATE.md"
      to: "docs/phase0-evaluation-2026-05-XX.md"
      via: "STATE.md links to evaluation doc as the M1 closure artifact"
      pattern: "phase0-evaluation"
---

<objective>
Roll up all Phase 0 measurements (Plans 02 and 04) and probes (Plan 03) into a single evaluation document. Apply PRD §6.2 Phase 0 kill criteria. Publish a binary go/no-go decision for Phase 1. Update STATE and ROADMAP to reflect the outcome.

Purpose: This is the M1 deliverable per ROADMAP and PRD §10.1. Phase 0 exists for one reason: produce a binary go/no-go on Phase 1. Plans 02–04 produce evidence; this plan produces the decision. The decision is the unit of value.

Output: `docs/phase0-evaluation-2026-05-XX.md` (≥200 lines, structured per PRD §10.1 deliverable spec); STATE.md and ROADMAP.md updated; Linear M1 milestone closed; Linear DEV-1052 closed; Phase 3 (Predictive-Delta ASR) either unblocked or paused per the verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/audiolayer-technical-prd-v0_2-2026-05-07.md
@.planning/phases/02-phase-0-mimi-feasibility-spike/02-01-HARDWARE-PATH-DECISION.md
@.planning/phases/02-phase-0-mimi-feasibility-spike/02-01-SUMMARY.md
@.planning/phases/02-phase-0-mimi-feasibility-spike/02-02-SUMMARY.md
@.planning/phases/02-phase-0-mimi-feasibility-spike/02-03-SUMMARY.md
@.planning/phases/02-phase-0-mimi-feasibility-spike/02-04-SUMMARY.md
@spike/bench/results/mimi-decode-mi300x.json
@spike/bench/results/mimi-decode-strix-derated.json
@spike/bench/results/vram-4-concurrent-mi300x.json
@spike/bench/results/vram-4-concurrent-strix-derated.json
@spike/bench/results/ffmpeg-pytorch-mi300x.json
@spike/bench/results/llm-on-tokens-eval.json
@spike/probes/pipecat_livekit_probe.md
@spike/probes/ffmpeg_pytorch_probe.md
@spike/probes/llm_on_tokens_probe.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write Phase 0 evaluation document with binary go/no-go</name>
  <files>docs/phase0-evaluation-2026-05-XX.md</files>
  <read_first>
    - PRD §6.1 (success metrics SM-AL-1, SM-AL-2); §6.2 (Phase 0 kill criteria); §7 (NC-AL-1, NC-AL-2, NC-AL-3, NC-AL-11); §10.1 (deliverables).
    - PRD §14 honest-uncertainty section — apply this framing to the verdict.
    - All four prior plan SUMMARYs and the result JSON files in `<context>`.
    - The hardware-path decision document `02-01-HARDWARE-PATH-DECISION.md` (the derating-confidence story).
    - PRD §15 closure-log pattern (Phase 1 closure log uses a tabular format that maps Linear → Decision Doc → Outcome — mirror this discipline here).
  </read_first>
  <action>
**Filename note:** replace `XX` in the filename with the actual day-of-month. Use `date +%Y-%m-%d`. The existing `docs/audiolayer-technical-prd-v0_2-2026-05-07.md` shows the established date-stamp convention.

The document is the M1 milestone deliverable. It must be optimized for two readers: the operator (Dustin) who needs to make a Phase 1 commit/pause decision, and a future reviewer (e.g., board member, partner firm engineering lead) who needs to verify the decision was made on evidence.

Required structure (concrete, not placeholders):

**Front matter (10 lines)** — use the same `---` YAML frontmatter delimiters as `docs/audiolayer-technical-prd-v0_2-2026-05-07.md`. Required keys (each on its own line inside the frontmatter block):

```
phase: 0
milestone: M1
project: thUMBox Audio Layer
prd_version: v0.2 (2026-05-07)
spike_window: <start-date> to <end-date>
hardware_path: <path-cloud | path-hardware>  # from 02-01-HARDWARE-PATH-DECISION.md
operator: Dustin
status: Accepted
```

**§1. TL;DR** (5–8 lines)
First three lines are the verdict. Either:
- "**GO** — Phase 1 (Predictive-Delta ASR) is recommended. All Phase 0 success criteria met or marginal; no PRD §6.2 kill criterion triggered."
- "**NO-GO** — Phase 1 is paused / project killed. PRD §6.2 kill criterion <X> triggered."
- "**MIXED** — Phase 1 is conditionally recommended pending <specific item>. Operator decision required at <date>."
Then a line per Phase 0 measurement: SM-AL-1 (decode latency), SM-AL-2 (LLM-on-tokens), NC-AL-1 (VRAM), NC-AL-2 (framework), NC-AL-3 (codec viability), NC-AL-11 (FFmpeg/PyTorch). One line each: criterion name + raw measurement + Strix prediction + verdict.

**§2. Methodology** (~20 lines)
- Hardware path chosen and why (cite `02-01-HARDWARE-PATH-DECISION.md`).
- Spike repo location, total LOC, model revisions pinned (cite `spike/configs/models.lock.yaml`).
- Reproducibility statement: how to re-run from clean checkout.
- Derating methodology if Path Cloud (cite `spike/src/spike/derate.py` and RBOX §7).

**§3. Measurement Results** (~60–80 lines)

A subsection per measurement. Each subsection has the same skeleton:

  - PRD criterion (verbatim quote from §6.1 or §7).
  - Kill criterion (verbatim quote from §6.2).
  - Raw measurement (cite JSON file path, give p50/p90/p99 or peak/per-model breakdown).
  - Strix Halo prediction (cite derated JSON, give value + CI).
  - Verdict per criterion: PASS / FAIL / INCONCLUSIVE / MARGINAL.
  - Honest-uncertainty note (e.g., "derate CI straddles 100 ms threshold; production Ollama overhead unmodeled").

The six measurements:
  - §3.1 SM-AL-1 (Mimi decode latency) — from Plan 02 Task 1.
  - §3.2 NC-AL-1 (VRAM at 4 concurrent) — from Plan 02 Task 2.
  - §3.3 NC-AL-2 (Pipecat/LiveKit framework support) — from Plan 03 Task 1.
  - §3.4 NC-AL-11 (FFmpeg/PyTorch streaming) — from Plan 03 Task 2.
  - §3.5 SM-AL-2 (LLM-on-tokens) — from Plan 04. **Reproduce the adapter-untrained caveat verbatim.**
  - §3.6 NC-AL-3 (codec viability on Strix Halo) — synthesized from §3.1 result.

**§4. Phase 0 Kill Criteria Application** (~15 lines)

Reproduce the three PRD §6.2 Phase 0 kill criteria verbatim. For each, cite the relevant §3.X subsection's verdict. Make the kill-decision logic explicit: "Per PRD §6.2 the project terminates if any of the following hold: ..."

**§5. Recommendation** (~20–30 lines)

If GO:
  - Phase 1 prerequisites (cite specific PRD §10.2 deliverables, ROADMAP Phase 3 success criteria).
  - Open risks Phase 1 must address (e.g., Ollama-vs-vLLM derating gap from RBOX §7).
  - Resource estimate (engineering days; reference NC-AL-2 framework probe finding for cost-shape).
  - Suggested Linear M2 (Phase 1) issue scoping shifts.

If NO-GO:
  - Which kill criterion triggered.
  - Project pause vs project kill recommendation.
  - What evidence would re-open the decision (e.g., "if Strix Halo dev unit lands and direct measurement shows decode <100 ms, re-evaluate").
  - Sunk cost summary (cloud spend, engineering days).

If MIXED:
  - The specific item that gates the decision.
  - The decision deadline.
  - The path forward in each branch.

**§6. Phase 1 Implications** (~10 lines)

What did Phase 0 surface that materially changes Phase 1 plan? Concrete bullets, not generic. Examples:
  - "NC-AL-2 finding favors LiveKit; Phase 1 plan should drop the Pipecat-evaluation task."
  - "VRAM headroom is < 8 GB at 4 concurrent; Phase 1's predictor model size choice must respect this."
  - "FFmpeg/PyTorch handoff is < 1 ms; Phase 1 can keep the architectural separation per DR-AL-6."

**§7. Honest Uncertainty** (~10 lines)

Per PRD §14 framing, what does this evaluation NOT answer? Concrete bullets:
  - Production Ollama overhead vs vLLM/transformers cloud measurement.
  - Adapter training cost (we tested untrained adapter only).
  - Real PSTN audio vs synthetic / open-licensed corpus distribution shift.
  - Strix Halo BIOS-allocatable GPU memory fraction (we assumed 96 GB conservative).
  - 4-concurrent-call workload realism (we measured idle-state peak; production has bursts).

**§8. Sunk Cost & Lessons** (~5–10 lines)

- Cloud spend total (cite TensorWave/Vultr billing).
- Engineering days actual vs 2-week timebox.
- One paragraph: what worked, what would be done differently next time.

**§9. Next Actions** (~5 lines)

If GO:
  - Run `/gsd-plan-phase 3` for Phase 1 (Predictive-Delta ASR) — owner: Dustin, target start: <date>.
  - Update Linear M2 milestone status from `Blocked` to `Up Next`.

If NO-GO:
  - Update Linear project status: thUMBox Audio Layer → Paused / Cancelled.
  - Notify partner firm (corpus path holder per DEV-1042) of project status.

**§10. Closure Log** (table, ~10 rows)

Mirror PRD §15 format: `| Linear | Plan | Outcome |` covering all 8 DEV-1045..1052 issues + the M1 milestone close.

Total length target: 200–350 lines. The document is read-and-decided-on, not skimmed.

Commit: `docs(02): Phase 0 evaluation document with go/no-go decision (DEV-1052, M1 closure)`.
  </action>
  <verify>
    <automated>EVAL_FILE=$(ls docs/phase0-evaluation-2026-05-*.md 2>/dev/null | head -1); test -n "$EVAL_FILE" && [ $(wc -l < "$EVAL_FILE") -ge 200 ] && grep -E "^(\\*\\*GO\\*\\*|\\*\\*NO-GO\\*\\*|\\*\\*MIXED\\*\\*)" "$EVAL_FILE" && grep -q "mimi-decode-strix-derated\\.json" "$EVAL_FILE" && grep -q "vram-4-concurrent-strix-derated\\.json" "$EVAL_FILE" && grep -q "llm-on-tokens-eval\\.json" "$EVAL_FILE" && grep -q "PRD §6.2" "$EVAL_FILE"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/phase0-evaluation-2026-05-<DD>.md` with the actual date.
    - ≥ 200 lines.
    - All 10 sections (§1–§10) present.
    - §1 TL;DR has GO / NO-GO / MIXED tagged in the first three lines.
    - §3 cites every result JSON by path.
    - §4 reproduces PRD §6.2 Phase 0 kill criteria verbatim.
    - §5 has a concrete recommendation matching the §1 verdict tag.
    - §7 includes the SM-AL-2 adapter-untrained caveat.
    - §10 closure log table covers all 8 DEV-1045..1052 issues.
  </acceptance_criteria>
  <done>The Phase 0 deliverable per ROADMAP and PRD §10.1 is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator review of evaluation document and verdict</name>
  <files>docs/phase0-evaluation-2026-05-XX.md</files>
  <what-built>
The Phase 0 evaluation document (Task 1) with a binary GO / NO-GO / MIXED verdict against PRD §6.2 kill criteria. This is the M1 milestone deliverable. The verdict drives whether Linear M2 (Phase 1) opens, whether the project pauses, or whether a specific gating item must be resolved first.

Files produced:
- `docs/phase0-evaluation-2026-05-<DD>.md` — the evaluation
- All Plan 02–04 artifacts cited within
  </what-built>
  <how-to-verify>
1. Read the evaluation document end-to-end. Time budget: 20–30 minutes (this is the most important read of the entire phase).
2. Cross-check §3 verdicts against the cited JSON files. Each verdict must be defensible against its evidence.
3. Cross-check §4 against PRD §6.2 verbatim — no kill criterion was silently dropped or softened.
4. Verify §5 recommendation matches §1 TL;DR verdict tag (no hidden disagreement between sections).
5. Verify §7 honest-uncertainty section contains the SM-AL-2 adapter-untrained caveat.
6. Reflect: do you (Dustin) actually believe the recommendation? If not, where does the document fail to convince you?

If approved, proceed to Task 3. If issues:
- For wording/structure issues: list them; Claude revises and re-presents.
- For verdict disagreement (e.g., "I think NC-AL-1 should be MARGINAL not PASS"): articulate the disagreement; Claude either updates or pushes back with reasoning per the operator CLAUDE.md "honesty over agreement" rule.
  </how-to-verify>
  <action>
Operator reads the evaluation document end-to-end and applies the verification steps in `<how-to-verify>` above. Operator either approves or articulates disagreements; on approval, Task 3 proceeds.
  </action>
  <verify>
    <automated>EVAL_FILE=$(ls docs/phase0-evaluation-2026-05-*.md 2>/dev/null | head -1); test -n "$EVAL_FILE" && grep -q "Accepted" "$EVAL_FILE"</automated>
  </verify>
  <done>Operator has approved the verdict; the evaluation document's status frontmatter reads "Accepted"; Task 3 (STATE/ROADMAP closure) is unblocked.</done>
  <resume-signal>Reply with "approved" or describe issues / verdict disagreements.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md and ROADMAP.md to reflect Phase 2 closure</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - The approved evaluation document from Task 2.
    - Current `.planning/STATE.md` and `.planning/ROADMAP.md`.
    - PRD §15 closure-log pattern (the precedent for how Phase 1 closure was recorded).
  </read_first>
  <action>
**STATE.md updates** (per the verdict):

If GO:
  - "Current Status" section: `Phase: Phase 3 (Predictive-Delta ASR) — ready to plan` and `Last action: Phase 2 (Mimi Feasibility Spike) closed GO; Phase 0 evaluation accepted <date>; M1 milestone delivered.`
  - "Phase Progress" table: row 2 (Phase 2) status → ✅ Complete (<date>); row 3 (Phase 3) status → ⏳ Ready to plan.
  - New section "Phase 2 Outcomes" mirroring the existing "Phase 1 Outcomes" pattern: bullet per measurement with verdict, link to evaluation doc and key result JSONs.
  - "Recent Decisions": add a 2026-05-<DD> entry for the GO decision and key derating-confidence note.
  - "Next Action": `/gsd-plan-phase 3` to plan Phase 1 (Predictive-Delta ASR).

If NO-GO:
  - "Current Status" section: `Phase: paused — project halted at Phase 2 (M1) per Phase 0 evaluation` and reference the kill criterion that triggered.
  - "Phase Progress" table: row 2 status → ❌ Killed (<date>); rows 3–5 status → ⏳ Blocked on project re-open.
  - "Recent Decisions": add the NO-GO decision and rationale.
  - "Next Action": "Operator decision: re-open under what evidence? Or pause indefinitely?"

If MIXED:
  - "Current Status": `Phase: Phase 2 closed MIXED — gating item: <X>; decision deadline: <date>.`
  - "Recent Decisions": the MIXED verdict and the gating item.
  - "Next Action": resolve gating item; then re-evaluate.

**ROADMAP.md updates**:

The header table in the "Overview" section: Phase 2 row's "Gate" column gets a check or strike per verdict. The `## Phase 2: Phase 0 — Mimi Feasibility Spike` section gets a "Status:" line at the bottom (matching the existing Phase 1 pattern: `**Status:** ✅ **Complete (date)** — ...`) summarizing the verdict and citing the evaluation doc path.

The `## Phase 3` section "Depends on:" line is updated: replace "Phase 2 (M1) success criteria met" with "Phase 2 closed GO (<date>) — see `docs/phase0-evaluation-2026-05-<DD>.md`" if GO.

**Linear updates** (operator-side; Claude prepares the comment text):
  - DEV-1052 status = Delivered with comment: link to evaluation doc, verdict tag, M1 milestone closed.
  - All remaining DEV-1045..1051 verified at status = Delivered.
  - M1 milestone status: Done (if GO/MIXED) or Cancelled (if NO-GO).
  - M2 milestone status: open from Blocked → Up Next (if GO).

Commit: `chore(02): close Phase 2 in STATE/ROADMAP per evaluation verdict (DEV-1052, M1 closure)`.
  </action>
  <verify>
    <automated>grep -E "Phase 2 \(Phase 0|Phase 0 evaluation" .planning/STATE.md && grep -E "(Complete|Killed)" .planning/ROADMAP.md && (grep -q "phase0-evaluation-2026-05" .planning/STATE.md || grep -q "phase0-evaluation-2026-05" .planning/ROADMAP.md)</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md reflects Phase 2 closure with the verdict and a link to the evaluation document.
    - ROADMAP.md Phase 2 row shows ✅ Complete or ❌ Killed (or MIXED with explanation) per the verdict.
    - ROADMAP.md Phase 3 row's `Depends on:` line is updated to reference the evaluation document if GO.
    - Linear DEV-1052 closed at status = Delivered.
    - All 8 DEV-1045..1052 issues verified at status = Delivered.
    - M1 milestone status closed (Done or Cancelled per verdict).
  </acceptance_criteria>
  <done>Phase 2 (M1 — Phase 0 Mimi Feasibility Spike) is closed in the GSD execution layer and in Linear, with the verdict propagated to STATE and ROADMAP. Phase 3 (Predictive-Delta ASR) is unblocked or paused per the decision.</done>
</task>

</tasks>

<verification_criteria>
- Evaluation document exists, ≥200 lines, all 10 sections present, verdict tagged in TL;DR.
- All Phase 0 result JSONs cited by path; no measurement is summarized away.
- PRD §6.2 kill criteria reproduced verbatim and applied per criterion.
- SM-AL-2 adapter-untrained caveat propagated to evaluation §3.5 and §7.
- Operator approves the verdict in Task 2.
- STATE.md and ROADMAP.md reflect the closure; Linear M1 milestone closed; DEV-1052 delivered.
</verification_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluation document → operator decision | Document is the artifact the GO/NO-GO commitment hangs on; misframing it is the highest-leverage failure mode of this plan |
| Linear status changes → external readers | Partner firm / board read M1/M2 status; misleading status creates downstream commitment errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Repudiation | Verdict justification | mitigate | §3 of evaluation cites every result JSON by path; §4 reproduces PRD §6.2 verbatim; §10 closure log links each Linear issue to the plan/task that delivered it |
| T-02-19 | Tampering | Post-hoc verdict adjustment | mitigate | Operator review (Task 2) is a blocking checkpoint; verdict change after Task 2 requires a new commit with explicit rationale and operator re-approval |
| T-02-20 | Information disclosure | Cloud spend / billing | accept | Cloud spend total is operationally non-sensitive; useful as Phase 1 cost-shape input |
| T-02-21 | Spoofing | "GO" recommendation under marginal evidence | mitigate | If any §3 verdict is INCONCLUSIVE / MARGINAL, §1 TL;DR cannot be plain GO — must be MIXED with the gating item named; verify check (Task 2) calls this out |
</threat_model>

<success_criteria>
- Phase 0 evaluation document committed and operator-approved.
- Binary go/no-go decision is unambiguous in the document and reflected in STATE/ROADMAP.
- All 8 Linear issues (DEV-1045..1052) closed at status = Delivered.
- M1 milestone closed (Done or Cancelled).
- If GO: Phase 3 (Predictive-Delta ASR) is unblocked; `/gsd-plan-phase 3` is the documented Next Action.
- If NO-GO: project pause/kill rationale documented; partner firm notification action item logged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-phase-0-mimi-feasibility-spike/02-05-SUMMARY.md` recording:
- Final verdict (GO / NO-GO / MIXED) with one-paragraph rationale.
- Path to the evaluation document.
- Status of all 8 DEV-1045..1052 issues and the M1 milestone.
- Total Phase 0 cloud spend (for cost-shape reference).
- Total engineering days spent vs the 2-week timebox.
- One paragraph: lessons that should propagate to the Phase 3 (Predictive-Delta ASR) plan.
</output>

